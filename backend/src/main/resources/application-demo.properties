# Lightsail / demo profile. JWT still comes from env JWT_SECRET (see application.properties).
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/player_db
spring.datasource.username=root
spring.datasource.password=

spring.jpa.show-sql=false
app.cookie.secure=true
